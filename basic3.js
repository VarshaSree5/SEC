function printNameAndScore(name, score) {
  console.log(`Name: ${name}, Score: ${score}`);
}

// Call the function with values
printNameAndScore("Alice", 90);


function rectangle(length, breadth){
    console.log('Perimeter: ${2*  (length + breadth)}');
}

rectangle(15,20);